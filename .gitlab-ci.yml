stages:
  - publish

publish:
  image: node:16
  stage: publish
  only:
    - master
  tags:
    - dind
  before_script:
    - echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > ~/.npmrc
  script:
    - npm publish --access=public
